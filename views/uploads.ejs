<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  
  <title>Audio Uploads</title>
</head>
<body>
  <div class="container">
    <nav>
      <div class="nav-wrapper">
        <div class="nav blue">
        <a href="/" class="brand-logo">Home</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
          <li>
          <form action="/logout" method="GET">
            <button type="submit">Log Out</button>
          </form>
          </li>
        </ul>
      </div>
      </div>
    </nav>

    <h3>Audio Uploads</h3>
    <%= typeof msg != 'undefined' ? msg : '' %>
    <form action="/uploads" method="POST" enctype="multipart/form-data">
      <div class="file-field input-field">
        <div class="btn grey">
          <span>File</span>
          <input name="myAudio" type="file">
        </div>
        <div class="file-path-wrapper">
          <input class="file-path validate" type="text">
        </div>
      </div>
      <button type="submit" class="btn" id="sbt">Submit</button>
    </form>
    <br>
    <audio src="<%= typeof file != 'undefined' ? file : '' %>" preload="auto" controls></audio>
    <br>
    <br>
    <h5>Audio Info</h5>
    <p>File name: <%= typeof filename != 'undefined' ? filename : '' %></p>
    <p>Encoding: <%= typeof encoding != 'undefined' ? encoding : '' %></p>
    <p>Size: <%= typeof size != 'undefined' ? size : '' %></p>
    <p>Timestamp: <%= typeof timestamp != 'undefined' ? timestamp : '' %></p>
    
  </div>

  <script src="https://code.jquery.com/jquery-3.6.1.min.js" integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>